services:
  brain-backend:
    build: ./backend-node
    env_file: .env
    environment:
      NODE_ENV: production
    networks:
      - brain
      - web
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3100/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  brain-frontend:
    build: ./frontend
    networks:
      - brain
      - web
    depends_on:
      brain-backend:
        condition: service_healthy
    restart: unless-stopped

networks:
  # Rede interna brain (frontend <-> backend)
  brain:
    driver: bridge

  # Rede compartilhada com infra-nginx, postgres e redis
  # Criada pelo /opt/infra/docker-compose.yml
  web:
    external: true
    name: infra-network
